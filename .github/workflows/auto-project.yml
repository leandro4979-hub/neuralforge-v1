name: Auto Project Sync

on:
  push:
    branches: [ main ]
  issues:
    types: [opened, closed, reopened]

jobs:
  update-project:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Log event
        run: echo "Event triggered for NeuralForge project"